# Abbreviation expansion map — append as new client datasets reveal new patterns.
# Keys are normalized (lowercased) abbreviations found in column names.
# Values are the canonical names used in candidate lists.

wk_end: week_end_date
wk: week
mth: month
qtr: quarter
yr: year
trx: trx
nbrx: nbrx
nbx: nbrx
rx: trx
prod: product
pt: patient
hcp: provider
geo: territory
terr: territory
prdct: product
prscbr: prescriber
phrmcy: pharmacy
pat: patient
diag: diagnosis
dx: diagnosis
ndx: nbrx
payer_chnl: payer_channel
chnl: channel
mkt: market
seg: segment
rx_dt: fill_date
svc_dt: service_date
fill_dt: fill_date
srvc_dt: service_date
persistency_pct: persistency
adhr: adherence_ratio
adh: adherence_ratio
dot: days_on_therapy
lot: line_of_therapy
# Veeva field aliases — map to canonical _id names so the key-suffix detector fires
npi_vod: npi_id          # Veeva NPI field (double) — is an HCP identifier
crm_user_id: crm_user_id # already ends _id; explicit for clarity

# CRM G1_ prefixed fields (Veeva CRM naming convention)
g1_meeting_type: meeting_type       # → segment via meeting_type candidate
g1_interaction_type: interaction_type  # → segment via interaction_type candidate

# Note: WAC (Wholesale Acquisition Cost) is a direct entry in measure_candidates
# — no abbreviation expansion needed; exact match gives confidence 1.0
